#include "monty.h"


/**
 * _nop_ - a function for the opcode nop doesn’t do anything
 * @stack: the input stack
 * @line_number: the line number
 */
void _nop_(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
